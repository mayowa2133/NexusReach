[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.renderer"

[deploy]
startCommand = "celery -A app.renderer_app:renderer_app worker --loglevel=info --concurrency=1 -Q render"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
