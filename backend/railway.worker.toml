[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "celery -A app.tasks worker --loglevel=info --concurrency=1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
