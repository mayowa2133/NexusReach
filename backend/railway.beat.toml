[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'export NEXUSREACH_SERVICE_ROLE=beat; exec celery -A app.tasks beat --loglevel=info'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
